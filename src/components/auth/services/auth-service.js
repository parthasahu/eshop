import axios from 'axios'

const API_BASE_URL = '';

export default class AuthService { 
    static api = {
      singIn(email, password) {
       
      },

      signOut() {
        
      }
    }
}